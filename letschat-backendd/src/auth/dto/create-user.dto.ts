/* eslint-disable prettier/prettier */
export class CreateUserDto {
    username: string;
    password: string;
    // Add any other properties required for user registration (e.g., email, firstName, lastName)
  }